import { View, Text } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const Profile = () => {
  return <SafeAreaView className="bg-black flex-1 p-5"></SafeAreaView>;
};

export default Profile;
